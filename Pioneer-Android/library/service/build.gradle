apply from: "../basic.gradle"

dependencies {
    api project(path: BuildModules.libraryBase)
    api project(path: BuildModules.libraryCommon)
    api project(path: BuildModules.libraryRouter)
    DependenciesEach.INSTANCE.processors.each { kapt it }

}
